#include <cstdio>
#include <iostream>
using namespace std;
int  main(){
	int n,k;
	ios::sync_with_stdio(false);
	cin.tie(0);

	cin>>n>>k;

	int t=0;
	//get bit of dex num
	for(int i=0;i<n;i++){
		int m=i;
		int bit_sum=0;
		
		if(bit_sum<=k)t++;

	}
	return 0;
}